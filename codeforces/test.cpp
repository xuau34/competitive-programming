#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int N, M;

void ini( void ){

}

int main(void){
  ios::sync_with_stdio(false);
  cin >> N;
  if( N - 2 <= 0 || (N - 2) & 1 ) cout << "NO" << endl;
  else cout << "YES" << endl;
}
