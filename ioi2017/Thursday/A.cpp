#include <bits/stdc++.h>
using namespace std;
int T, N, M, Q;

int main(void){
  int i, j;
  scanf("%d", &T);
  while(T--){
    scanf("%d", &N);
    if(N <= 3 || N >= 5) printf("1\n");
    else printf("-1\n");
  }
}
